import $ from "jquery";

const rootApp = document.getElementById("root");
rootApp.innerHTML = '<button id="toggleButton">ON</button>'; 

$("#toggleButton").click(() => {
  if ($("#toggleButton").html() === "ON") {
    $("#toggleButton").html("OFF");
  } else {
    $("#toggleButton").html("ON");
  }
});
